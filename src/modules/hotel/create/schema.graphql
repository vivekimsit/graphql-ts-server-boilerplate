scalar Upload

input CreateHotelInput {
  name: String!
  description: String!
  picture: Upload
  latitude: Float!
  longitude: Float!
}

type Mutation {
  createHotel(input: CreateHotelInput!): Boolean!
}
